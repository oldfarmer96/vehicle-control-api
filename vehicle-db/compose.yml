services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: lain
      POSTGRES_PASSWORD: 3322
      POSTGRES_DB: controldb
    ports:
      - '6000:5432'
    volumes:
      - vehicle-pgdata:/var/lib/postgresql/data

volumes:
  vehicle-pgdata:
